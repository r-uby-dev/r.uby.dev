{
  "private": true,
  "scripts": {
    "build:css": "esbuild app/assets/css/main.css --bundle --minify --legal-comments=eof --outfile=public/css/main.css",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
